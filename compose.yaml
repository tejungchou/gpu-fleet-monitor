services:
  simulator:
    build: .
    container_name: gpu-simulator
    volumes:
      - .:/app

  receiver:
    image: python:3.12-slim
    container_name: receiver
    working_dir: /app
    volumes:
      - ./receiver:/app
    command: python receiver.py
